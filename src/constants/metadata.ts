import { Metadata } from 'next';

const title = 'z1pl';
const description = 'z1zon playground';

export const DEFAULT_METADATA: Metadata = {
  title,
  description,
};

const METADATA = {
  title,
  description,
};

export default METADATA;
